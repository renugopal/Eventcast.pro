# ⚙️ EVENTCAST PRO - NPM CONFIGURATION FOR CLOUDFLARE PAGES
# This ensures that peer dependency conflicts (like Next.js version differences) 
# are automatically bypassed during Cloudflare CI/CD npm install.

legacy-peer-deps=true
