# infra/media-node/compose

Docker Compose definitions for SRS, the EventCast Media Agent, and supporting agents on a single media node. All images must be pinned by tag plus digest; host directories, SQLite, and spool data must persist independently of containers (see `../../../04_TECH_STACK_AND_VERSION_POLICY.md`, "Deployment runtime").

No Compose files exist yet — placeholder created during the Phase 0 repository baseline.
