# infra/media-node/srs

Pinned SRS configuration (`ossrs/srs:v6.0-r0` or its verified immutable digest). See `../../../04_TECH_STACK_AND_VERSION_POLICY.md` for the version-pin rationale and `../../../03_DATA_MODEL_AND_API_CONTRACTS.md` for the required `on_publish` / `on_hls` / `on_unpublish` callback contracts this config must wire up to the Media Agent.

No configuration files exist yet — placeholder created during the Phase 0 repository baseline.
