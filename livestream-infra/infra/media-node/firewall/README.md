# infra/media-node/firewall

Host firewall rules for a media node: public RTMP ingest port, loopback/private-network-only Media Agent HTTP (per `../../../04_TECH_STACK_AND_VERSION_POLICY.md`, "EventCast Media Agent"), and monitoring egress.

No rule files exist yet — placeholder created during the Phase 0 repository baseline.
